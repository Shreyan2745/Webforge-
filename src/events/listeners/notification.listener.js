// Maps domain events to user notifications (in-app + email).
// TODO(build: notifications): REGISTRATION_CONFIRMED, WAITLIST_JOINED, WAITLIST_PROMOTED, REGISTRATION_CANCELLED,
//   MARKED_NO_SHOW, WORKSHOP_UPDATED, WORKSHOP_CANCELLED, WORKSHOP_REMINDER, CERTIFICATE_AVAILABLE

module.exports = function registerNotificationListener() {};
